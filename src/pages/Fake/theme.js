import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ffffff', // Color de fondo de la barra de navegación
    },
    text: {
      primary: '#000000', // Color del texto
    },
  },
});

export default theme;
